# 蔚蓝博客 · Linux 部署指南（宝塔 / 1Panel 通用）

纯 Node.js 实现，**零 npm 依赖**，Node ≥ 22.13（推荐 24 LTS，需要内置 SQLite 模块）。所有数据保存在**单个 SQLite 数据库文件** `data/blog.db`（含用户、文章、评论、商店、抽奖、主题、设置、登录会话），备份该文件 = 备份整站。

> 首次启动自动创建管理员：`admin / admin123`，登录后台后请立即改密！
> 旧版本 JSON 文件存储的数据（data/*.json）会在启动时**自动迁移**进数据库并改名为 `.bak` 保留。

---

## 方式一：宝塔面板

### 1. 安装 Node 环境
软件商店 → 搜索并安装 **「Node.js版本管理器」** → 打开它安装 Node **24.x**（或 22.13 以上任意版本）。

### 2. 上传代码
文件 → 进入 `/www/wwwroot/` → 上传 `blog.zip` → 解压，得到如 `/www/wwwroot/blog`。

### 3. 创建 Node 项目
- 新版宝塔（≥7.9）：**网站 → Node项目 → 添加Node项目**
  - 项目目录：`/www/wwwroot/blog`
  - 启动选项：`npm start`（或启动文件选 `server.js`）
  - 端口：`3000`
  - 勾选「开机自启」和「守护进程」（底层用 PM2）
- 旧版宝塔：安装软件商店的 **「PM2管理器」** → 添加项目 → 项目目录选 `/www/wwwroot/blog`，启动文件 `server.js`，项目名 `blue-blog`。
  或 SSH 执行：`cd /www/wwwroot/blog && pm2 start ecosystem.config.js && pm2 save`

### 4. 绑定域名（可选）
网站 → 添加站点（填你的域名，纯静态即可）→ 站点设置 → **反向代理** → 目标 URL 填 `http://127.0.0.1:3000`。
之后用 `http://你的域名` 访问；不上域名就直接 `http://服务器IP:3000`（记得防火墙/安全组放行 3000 端口）。

---

## 方式二：1Panel

### 推荐：Docker Compose（1Panel 对 Docker 支持最好）
1. 文件 → 上传 `blog.zip` 到如 `/opt/blog` 并解压（docker-compose.yml 需在同目录）。
2. **容器 → 编排 → 创建编排** → 选择该目录（或直接「容器 → 主机 → SSH」执行）：
   ```bash
   cd /opt/blog && docker compose up -d --build
   ```
3. 访问 `http://服务器IP:3000`；反向代理：网站 → 创建网站 → 反向代理 → 代理地址 `http://127.0.0.1:3000`。

### 备选：Node 运行环境
网站 → **运行环境 → Node.js → 创建**：
- 源码目录：`/opt/blog`
- 启动命令：`npm start`（或 `node server.js`）
- 应用端口：`3000`
- 创建后在运行环境详情里可看日志/重启；再按需建反向代理网站。

---

## 方式三：裸机 systemd（不依赖面板）

```bash
# 假设已装 Node 18+（apt/dnf 均可，或 nvm）
unzip blog.zip -d /opt/blog && cd /opt/blog
cat > /etc/systemd/system/blue-blog.service <<'EOF'
[Unit]
Description=Blue Blog (pure Node.js)
After=network.target

[Service]
WorkingDirectory=/opt/blog
ExecStart=/usr/bin/node server.js
Environment=PORT=3000
Restart=always
User=root

[Install]
WantedBy=multi-user.target
EOF
systemctl enable --now blue-blog
systemctl status blue-blog   # 查看状态，journalctl -u blue-blog 看日志
```

---

## 环境变量（可选）

| 变量 | 默认 | 说明 |
|---|---|---|
| `PORT` | 3000 | 监听端口 |
| `KOMARI_URL` | 空 | komari 面板地址（简单场景用；多数据源在后台「设置」里配） |
| `PROBE_SOURCES` | 空 | 探针数据源 JSON 数组，如 `[{"provider":"nezha","url":"http://x","token":"t"}]` |
| `MC_SERVERS` | localhost:25565 | MC 服务器查询列表，逗号分隔 |

---

## 升级与备份

- **升级**：上传覆盖代码文件 → 面板里重启项目（或 `pm2 restart blue-blog` / `docker compose up -d --build`）。`data/` 目录不要覆盖，数据库在里面。
- **备份**：备份 `data/blog.db` 一个文件即可（建议停止服务时拷贝；运行中备份请连同 `blog.db-wal`、`blog.db-shm` 一起打包，或用 `sqlite3 blog.db ".backup backup.db"` 在线备份）。
- **恢复**：停止服务 → 用备份文件覆盖 `data/blog.db` → 启动。
- **换端口**：改环境变量 `PORT`，同步修改反向代理目标。

## 常见问题

- **502/打不开**：先 `curl http://127.0.0.1:3000` 确认服务本身活着，再查反代配置和防火墙（宝塔「安全」、云厂商安全组都要放行）。
- **komari 探针显示"未配置"**：后台 → 设置 → 填 komari 面板地址；Docker 部署时若 komari 也在本机容器里，地址填容器名或宿主 IP，不要填 `localhost`。
- **MC 状态一直离线**：MC 服务端需在 `server.properties` 开启 `enable-query=true` 并放行 Query 端口（默认同 25565/UDP）。
